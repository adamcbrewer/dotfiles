# Codex Usage dependencies

- Python 3
- An authenticated Codex CLI available on the Omarchy shell's `PATH`

No API key environment variable is required. Missing or unavailable Codex
usage data is reported by the widget without breaking the shell.
